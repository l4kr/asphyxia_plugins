//DATA//
  profile: DB.Find(refid, { collection: 'profile' })
  score: DB.Find(refid, { collection: 'music' })

-
  //const padded = _.padStart(profile.id.toString(), 8);
  //const sdvxid = `${padded.slice(0, 4)}-${padded.slice(4)}`;

link(rel="stylesheet" href="static/asset/css/datatables.css")
link(rel="stylesheet" href="static/asset/css/score_modal.css")
div
  .card
    .card-header
      p.card-header-title
        span.icon
          i.mdi.mdi-account-edit
        | Score
    .card-content
      .field
            label.label Show scores for version:
            .ver-select
              .control
                .select
                  select(name="version_select" id="version_select")
      table.table(id="music_score" style="width:100%")
        thead
          tr    
            th ID
            th Song Name
            th Difficulty
            th Score
            th Exscore
            th Grade
            th Clear Medal
            th Last Played
        tbody

div.modal#score-detail-modal
  div.modal-background(onclick="closeScoreModal()")
  div.modal-content.score-modal-content
    div.score-header
      img.score-header-jacket#modal-jacket(src="" style="display:none" alt="")
      div.score-header-text
        h2#modal-songname Song Name
        div.score-subtitle
          span.diff-badge#modal-diff MXM
        div.score-played-by
          span.score-played-by-text Played by
          span.score-played-by-name#modal-player-name Player
    
    div.score-body
      div.score-rank-container
        div#modal-rank(data-grade="S") S

      div.score-stats
        div.score-number#modal-score 9,993,855
        div.score-meta-row
          div.score-meta-item
            span.score-meta-label EX Score
            span.score-meta-value#modal-exscore 0
          div.score-meta-item
            span.score-meta-label Max Combo
            span.score-meta-value#modal-maxchain 0
        div.score-medal-row
          span#modal-medal PLAYED

      div.judgements-container
        div.judgements-title Judgements
        div.judgement-row.j-row-scrit
          span.j-dot
          span.j-label S-CRITICAL
          span.j-value#modal-scrit 0
        div.judgement-row.j-row-crit
          span.j-dot
          span.j-label CRITICAL
          span.j-value#modal-crit 0
        div.judgement-row.j-row-near
          span.j-dot
          span.j-label NEAR
          span.j-value#modal-near 0
        div.j-sub-row
          span.j-sub-item
            span.arrow-early ◀
            span#modal-early 0
          span.j-sub-item
            span#modal-late 0
            span.arrow-late ▶
        div.judgement-row.j-row-error
          span.j-dot
          span.j-label ERROR
          span.j-value#modal-error 0

  button.modal-close.is-large(aria-label="close" onclick="closeScoreModal()")

div(hidden id='profile-pass') !{JSON.stringify(profile)}
div(hidden id='score-pass') !{JSON.stringify(score)}
script(src="static/asset/js/score.js") 
script(src="static/asset/js/datatables.js")